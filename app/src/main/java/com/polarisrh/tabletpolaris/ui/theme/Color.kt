package com.polarisrh.tabletpolaris.ui.theme

import androidx.compose.ui.graphics.Color

// Dark navy background, matches the Polaris RH web platform
val PolarisBackground = Color(0xFF0A0E1A)
val PolarisSurfaceDark = Color(0xFF121729)

// Accent blues, sampled from the Polaris RH "P" mark and highlighted copy
val PolarisBlue = Color(0xFF4C7DFF)
val PolarisBlueDeep = Color(0xFF16215C)
val PolarisTeal = Color(0xFF12A594)

// Light card used for the code-entry panel, matches the login card on the web platform
val PolarisCard = Color(0xFFEDEFF5)
val PolarisOnCard = Color(0xFF10142B)

val PolarisOnPrimary = Color(0xFFFFFFFF)
val PolarisError = Color(0xFFEB5757)
val PolarisWarning = Color(0xFFF2994A)
val PolarisSuccess = Color(0xFF27AE60)
val PolarisMuted = Color(0xFF8A93B2)

// Stand-in for the live camera feed area, until CameraX is wired in
val PolarisCameraPlaceholder = Color(0xFF12151F)
